root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main cmd/main.go"
bin = "./tmp/main"
include_ext = ["go", "yaml"]
exclude_dir = ["tmp", "vendor", "tests"]
delay = 1000 # ms

[screen]
clear_on_rebuild = true
